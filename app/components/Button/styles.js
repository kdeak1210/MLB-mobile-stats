import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonRow: {
    paddingBottom: 20,
    flexDirection: 'row',
    alignItems: 'flex-start'
  },
  button: {
    alignItems: 'center',
    backgroundColor: '#000',
    padding: 14,
    margin: 16,
    borderRadius: 8
  },
  buttonText: {
    color: '#fff',
    fontSize: 14,
    fontWeight: '600'
  }
});
