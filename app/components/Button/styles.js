import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonRow: {
    paddingBottom: 10,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center'
  },
  button: {
    alignItems: 'center',
    backgroundColor: '#000',
    padding: 14,
    margin: 16,
    borderRadius: 8
  },
  buttonText: {
    color: '#fff',
    fontSize: 14,
    fontWeight: '600'
  }
});
